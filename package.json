{
  "name": "qa-ai-agent-poc",
  "version": "1.0.0",
  "private": true,
  "description": "POC autonomous test engineering workflow (JIRA + MCP + AI + Playwright)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
